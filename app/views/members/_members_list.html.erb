<% unless @members.empty? %>
		
		<div class="memberInfo memberInfoList">
			<%= image_tag member.avatar(:thumb), :class => "left thumb" %>		
			<div>
				<%=link_to truncate(member.display_name), member_path(member) %>
				<p class="small">
				<% unless !member.active %>
					<%= "Member Since: "+ member.joined_at.strftime('%m/%d/%y')  if member.joined_at %><br>
				<% else %>
					not active<br>
				<% end %>
				<%= "Last Renewed: "+ member.last_renewed_at.strftime('%m/%d/%y') if member.last_renewed_at and current_user.admin? %>
				</p>
			</div>	
		</div>
		
<% end %>